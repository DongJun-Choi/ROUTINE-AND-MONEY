# AGENTS.md

## 우리 프로젝트의 내용

- 이 프로젝트는 카드 이용 내역을 자동으로 수집·정리하고 소비 패턴을 보여 주는 개인 재무 관리 웹 애플리케이션이다.
- 주요 기능은 카드 명세서 엑셀 업로드, 네이버 메일 첨부파일 동기화, 거래 내역 관리, 카테고리 및 자동 분류 규칙 관리, 월별·일별·카테고리별 대시보드다.
- 기술 스택은 Next.js App Router, React, TypeScript, Tailwind CSS, Prisma, PostgreSQL, Chart.js다.
- 화면과 API는 `src/app`, 공용 UI는 `src/components`, 도메인 및 외부 연동 로직은 `src/lib`, 데이터 모델과 마이그레이션은 `prisma`에 둔다.
- 사용자 금융 데이터와 메일 인증 정보를 다루므로 정확성, 중복 방지, 개인정보 보호를 우선한다.

## 명령어 (작업 후 반드시 검증)

```bash
npm install              # 의존성 설치
npm run dev              # Docker DB, 앱, 메일 동기화 워커를 함께 개발 모드로 실행
npm run dev:app          # Next.js 앱만 개발 모드로 실행
npm run build            # 프로덕션 빌드 및 타입 검사
npm run start            # Docker DB, 앱, 메일 동기화 워커를 함께 프로덕션 모드로 실행
npm run db:up            # Docker PostgreSQL 실행 및 헬스체크 대기
npm run db:status        # Docker PostgreSQL 상태 확인
npm run db:down          # Docker PostgreSQL 컨테이너 중지 (볼륨 유지)
npm run mail:worker      # 네이버 메일 동기화 워커만 실행
npx prisma validate      # Prisma 스키마 검증
npx prisma generate      # Prisma Client 재생성
```

- 모든 코드 작업 후 `npm run build`를 반드시 실행한다.
- Prisma 스키마를 변경했다면 `npx prisma validate`, `npx prisma generate`, `npm run build`를 모두 실행한다.
- 메일 동기화 로직을 변경했다면 관련 `npm run mail:test:*` 명령과 실제 대상 흐름을 추가로 확인한다. 이 명령은 개인 메일 및 환경 변수에 접근할 수 있으므로 안전한 테스트 환경에서만 실행한다.
- 자동화된 테스트가 없는 영역은 변경한 화면과 API의 정상 흐름, 빈 값, 오류 흐름을 직접 확인하고 검증 결과를 작업 설명에 남긴다.
- 검증에 실패하면 실패 사실과 원인을 숨기지 말고 기록한다.

## 코드 컨벤션

- 브랜치, 커밋, Pull Request, 코드 스타일, 검증 및 환경 변수에 관한 상세 규칙은 `docs/DEVELOPMENT_CONVENTIONS.md`를 따른다.
- `main`과 `develop`에 직접 푸시하지 않고 작업 브랜치와 Pull Request를 사용한다.
- 커밋과 Pull Request의 제목 및 설명은 한글로 작성하며 Conventional Commits 형식을 따른다.
- 커밋 메시지에 AI 도구의 `Co-Authored-By` 등 자동 서명을 추가하지 않는다.
- 새 코드는 TypeScript를 기본으로 하고 `tsconfig.json`의 strict 설정을 지킨다. 기존 JavaScript 파일은 관련 작업 범위에서만 점진적으로 TypeScript로 전환한다.
- 내부 모듈은 가능하면 `@/*` 경로 별칭을 사용한다.
- React 컴포넌트와 타입은 `PascalCase`, 함수·변수는 `camelCase`, 상수는 의미가 분명한 이름을 사용한다.
- 페이지와 Route Handler는 App Router 규칙을 따르고, 재사용 가능한 UI는 `src/components`, 비즈니스 로직과 외부 연동은 `src/lib`로 분리한다.
- 컴포넌트는 한 가지 책임에 집중하고, 데이터 조회·정제·저장 로직을 화면 컴포넌트에 직접 쌓지 않는다.
- API 입력은 신뢰하지 말고 필수 값, 타입, 범위를 검증한다. 오류 응답은 일관된 HTTP 상태 코드와 이해 가능한 메시지를 사용한다.
- 금액 계산은 부동소수점 오차가 생기지 않도록 현재 모델과 동일하게 정수 단위로 처리한다.
- 날짜와 월 단위 로직은 타임존 경계를 고려하며, 저장 형식과 화면 표시 형식을 명확히 구분한다.
- Prisma 모델 변경은 마이그레이션과 함께 관리한다. 이미 적용된 마이그레이션 파일을 수정하지 않는다.
- 주석은 코드가 무엇을 하는지 반복하지 말고, 정책·예외·의사결정의 이유를 설명할 때만 작성한다.
- 기존 파일의 스타일을 존중하고, 요청과 무관한 대규모 포맷 변경이나 리팩터링은 섞지 않는다.

## 문서 (Notion과 연동할 예정)

- Notion을 프로젝트 문서의 중심으로 연동할 예정이며, 연동 전까지 저장소의 `README.md`와 코드가 사실의 기준이다.
- 기능 변경 시 목적, 사용자 흐름, API·데이터 모델 영향, 환경 변수, 검증 방법을 함께 문서화한다.
- 중요한 기술 결정은 배경, 선택지, 결정, 이유, 영향이 드러나도록 기록한다.
- Notion 페이지 제목과 본문에는 관련 기능명, 저장소 경로, 이슈 또는 PR 링크를 포함해 추적 가능하게 만든다.
- 문서와 구현이 어긋나면 같은 작업에서 함께 갱신한다. 확정되지 않은 내용은 사실처럼 쓰지 말고 `초안`, `검토 필요`, `결정됨` 상태를 명시한다.
- 비밀번호, 토큰, 실제 금융 거래 데이터, 개인 메일 내용은 Notion·README·이슈·커밋·PR에 복사하지 않는다.
- 커밋 및 PR 설명은 한글로 작성한다.

## 하지 말 것

- `.env`나 인증 정보, 데이터베이스 접속 정보, 실제 금융·메일 데이터를 커밋하거나 로그에 노출하지 않는다.
- 사용자 확인 없이 운영 데이터 삭제, 전체 재분류, 마이그레이션 초기화, 강제 푸시 등 되돌리기 어려운 작업을 하지 않는다.
- 이미 적용된 Prisma 마이그레이션을 고치거나 데이터 손실 가능성이 있는 스키마 변경을 설명 없이 추가하지 않는다.
- 거래 중복 방지, 금액 부호, 거래 유형, 카테고리 계층 규칙을 우회하지 않는다.
- API 입력값이나 외부 메일·엑셀 데이터를 검증 없이 신뢰하지 않는다.
- 검증하지 않은 코드를 완료로 보고하지 않는다.
- 요청 범위를 벗어난 의존성 추가, 구조 개편, 파일 전체 재작성, 무관한 코드 정리를 하지 않는다.
- 테스트 목적으로 실제 메일의 읽음 상태를 바꾸거나 운영 데이터베이스에 샘플 데이터를 넣지 않는다.
